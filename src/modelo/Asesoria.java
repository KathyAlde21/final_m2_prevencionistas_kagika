package modelo;

/**
 * Interfaz Asesoria
 * Define el método analizarUsuario que deben implementar los usuarios del sistema.
 */
public interface Asesoria {
	/**
     * Método que permite analizar los datos del usuario.
     */
    void analizarUsuario();
}
